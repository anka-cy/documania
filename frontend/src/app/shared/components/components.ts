// Baril des composants partagés : import unique depuis les pages
// (showLoading, renderError, showToast, confirmAction…).

export { showLoading } from './loading';
export { renderEmpty } from './emptyState';
export { renderError } from './errorMessage';
export { showToast } from './toast';
export { confirmAction } from './confirmDialog';
export { renderPager } from './pagination';
